{
  "name": "churchtools-facts-reporter",
  "version": "0.1.0",
  "author": "Philipp Trenz",
  "description": "This app provides an easy way to report facts about events in ChurchTools",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/philipptrenz/ChurchTools-Facts-Reporter.git"
  },
  "os": [
    "win32",
    "darwin"
  ],
  "keywords": [
    "ChurchTools",
    "facts",
    "reporting"
  ],
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "./node_modules/.bin/electron-rebuild",
    "package-mac": "electron-packager . \"ChurchTools Facts Reporter\" --overwrite --platform=darwin --arch=x64 --icon=assets/icons/mac/icon.icns --prune=true --out=release-builds",
    "package-win": "electron-packager . \"ChurchTools Facts Reporter\" --overwrite --asar --platform=win32 --arch=x64 --icon=assets/icons/win/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"ChurchTools Facts Reporter\"",
    "package-linux": "electron-packager . \"ChurchTools Facts Reporter\" --overwrite --asar --platform=linux --arch=x64 --icon=assets/icons/linux/icon_1024x1024.png --prune=true --out=release-builds"
  },
  "dependencies": {
    "electron-json-storage": "^4.1.6",
    "jquery": "^3.4.1",
    "keytar": "^4.6.0",
    "materialize-css": "^1.0.0",
    "moment": "^2.24.0",
    "q": "^1.4.1",
    "restler": "^3.4.0",
    "underscore": "^1.8.3",
    "winston": "^2.3.1"
  },
  "devDependencies": {
    "electron": "^5.0.0",
    "electron-rebuild": "^1.8.4"
  }
}
